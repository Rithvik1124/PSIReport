{
  "build": {
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "streamlit run streamlit_app.py --server.port=8000 --server.address=0.0.0.0",
    "restartPolicyType": "ON_FAILURE"
  }
}
